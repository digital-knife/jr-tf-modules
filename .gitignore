# ==============================================================================
# TERRAFORM / TERRAGRUNT DEFAULTS
# ==============================================================================

# Local .terraform directories (recursive)
**/.terraform/*

# Terragrunt cache directories (recursive)
# This is where Terragrunt downloads modules; it can be gigabytes of data.
**/.terragrunt-cache/*

# Generated files
# Since we use 'generate' blocks, these files are transient and should not be tracked.
backend.tf
provider.tf

# .tfstate files
# Even though we use remote state, local backups can appear during migrations.
*.tfstate
*.tfstate.*

# Terraform plan files
# These are binary files and often contain sensitive data.
*.tfplan

# ==============================================================================
# LOGS AND SENSITIVE FILES
# ==============================================================================

# Crash logs
crash.log
crash.*.log

# Exclude sensitive variables files
*.tfvars
*.tfvars.json

# Exclude override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Exclude CLI configuration files
.terraformrc
terraform.rc

# Never track private keys
*.pem
*.pub
*.key
id_rsa*

# Local environment files containing secrets
.env
.envrc

# ==============================================================================
# OS / IDE SPECIFIC
# ==============================================================================

.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
